# Build Checklist

> **Spec-first cycle (Substrate (mm) pattern).** Built from [`docs/spec.md`](spec.md) + [`docs/prd.md`](prd.md). The design spec at [`docs/superpowers/specs/2026-05-03-macro-mac-app-design.md`](superpowers/specs/2026-05-03-macro-mac-app-design.md) is authoritative for substantive detail any item is silent on.

## Build Preferences

- **Build mode:** **Autonomous** *(default per `plugins.vibe-cartographer.autonomy_level: fully-autonomous`. Surfaced for builder confirmation in handoff message before `/build` runs.)*
- **Comprehension checks:** N/A (autonomous mode)
- **Git:** Commit after each checklist item with message format `feat(<area>): item N — <title>` for code items, `docs(<area>): item N — <title>` for doc-only items, `chore(<area>): item N — <title>` for infra items. Co-author trailer per repo convention.
- **Verification:** Yes — checkpoints after items 6, 8, and 9. At each checkpoint, agent pauses, summarizes what was built, and waits for builder green-light before continuing.
- **Check-in cadence:** N/A (autonomous mode)

## Sequencing Rationale

Three Key Technical Decisions from `spec.md` drive the order:

1. **Schema codegen first** (item 2) — every Swift file touching macros uses generated types from day one; zero schema drift.
2. **Engine before editor** (items 5–6 before 8) — the editor's "Run" button needs the engine; engine is testable against hand-authored YAML bundles without the editor.
3. **PS99 plugin authored last** (item 10) — engine + format must be game-agnostic; building PS99 first risks tight-coupling.

Item 6 (hand-authored test bundles) is a deliberate verification beat; it bridges items 5 (engine exists) and 7 (recorder exists) so the engine can be exercised against real fixtures before the recorder is built.

## Checklist

- [x] **1. Repo skeleton — Xcode project, app shell, `MacRoTheme` stub, schema source-of-truth**
  Spec ref: `spec.md > Stack` + `spec.md > File Structure`
  What to build: Create `App/macRo.xcodeproj` (macOS 14+ deployment target, SwiftUI, Universal binary). `App/macRo/App.swift` with `@main` and basic `AppDelegate`. `App/macRo/Info.plist` declaring `LSItemContentTypes` for the `.macro` bundle (UTI: `com.626labs.macro`, conforms to `public.directory`). Empty `App/macRo/Theme/MacRoTheme.swift` stub with placeholder Color/Font extensions referencing 626Labs tokens (full mapping comes via `626labs-design` skill at first SwiftUI authoring beat). `schema/macro.schema.yaml` with the v1 schema derived from spec § 4 (manifest fields, timeline event kinds, gates, factoryPatchable, target, requires, schedule, patchHistory, stopOn, subs).
  Acceptance: Xcode opens project. `cmd+B` builds. App launches to a blank SwiftUI window titled "macRo". `MacroFormat` import resolves. `python3 -c "import yaml; yaml.safe_load(open('schema/macro.schema.yaml'))"` exits 0.
  Verify: `xed App/macRo.xcodeproj` opens Xcode. Build succeeds. Run shows blank window. Schema YAML parses.
  **Status:** complete @ `256922a` + verified with Xcode 16.4 (`** BUILD SUCCEEDED **`). Ad-hoc local signing only; full hardened runtime + notarization land at item 11. Visual verification (cmd+R → blank window with brand glyph + tagline) is owed-to-Estevan whenever convenient.

- [x] **2. Schema codegen pipeline + `MacroFormat` module** *(complete @ `4acc624`. Hand-written Bun/TS codegen at `tools/codegen/`; emits 26K of Swift Codable types to `App/macRo/Schema/MacroFormat.swift` including the `TimelineEvent` discriminated-union enum. CI lockstep guard at `.github/workflows/ci.yml`. xcodebuild SUCCEEDED. Round-trip smoke test verified. TS factory-types codegen deferred to Subsystem B's cycle.)*
  Spec ref: `spec.md > Schema source of truth` + `spec.md > MacroFormat`
  What to build: Pick codegen tool (Quicktype recommended; alternatives: hand-written or Swift macros — pick at this item per spec Open Issues). Set up `tools/codegen/generate-types.ts` (or equivalent) that reads `schema/macro.schema.yaml` and emits `App/macRo/Schema/MacroFormat.swift` (Manifest, Timeline event types, Gates, Subs, etc. with `Codable` conformance). Add `.github/workflows/ci.yml` step that runs codegen, diffs against committed `MacroFormat.swift`, fails if drift. Document the regen command in `CLAUDE.md`.
  Acceptance: Editing `schema/macro.schema.yaml` and re-running codegen updates `MacroFormat.swift` consistently. Generated types are usable from Swift. CI lockstep guard catches drift (proven by intentionally drifting and confirming red CI in a draft PR).
  Verify: Modify schema (add a test field), run `bun run codegen`, confirm `MacroFormat.swift` reflects the change. Revert. Run CI script locally — exit 0. Drift the schema without regenerating — exit ≠0.

- [x] **3. Domain primitives — `MacroBundle` (load/save) + `Permissions` + `OnboardingView` (entitlements wizard)** *(complete @ `0ba38d6`. MacroBundle.swift (340 lines, full IO + cross-ref validation + typed errors), Permissions.swift (@Observable with frontmost-app observer), OnboardingView.swift (two-step wizard, MacRoTheme-routed). Yams added via SPM. macRoTests target wired; 5 tests pass (round-trip + negative cross-ref). BUILD SUCCEEDED + TEST SUCCEEDED. Visual cmd+R walkthrough owed-to-Estevan: reset macRo permissions in System Settings → cmd+R → walk wizard → grant both → confirm Library view appears → quit → revoke Accessibility → re-launch → confirm wizard returns. Decision worth logging: Universal-binary policy is Release-only; Debug is `ONLY_ACTIVE_ARCH=YES` due to Yams SPM Intel-host build choke.)*
  Spec ref: `spec.md > MacroBundle` + `spec.md > Permissions` + `spec.md > OnboardingView` + `prd.md > Epic A`
  What to build: `App/macRo/Domain/MacroBundle.swift` — load a `.macro` folder, parse `manifest.yaml` + `timeline.yaml` against `MacroFormat`, save by writing yaml + copying gate PNGs. Validation against schema with clear error messages. `App/macRo/Domain/Permissions.swift` — check + request Accessibility (`AXIsProcessTrusted` + `AXIsProcessTrustedWithOptions`) and Screen Recording (`CGRequestScreenCaptureAccess`); deep-link to System Settings panes via `x-apple.systempreferences://`. Frontmost-app-changed observer to re-check on foreground. `App/macRo/UI/OnboardingView.swift` — first-launch wizard implementing PRD epic A: welcome screen → entitlements wizard with per-permission cards (name, plain-English reason, real-time status, action button) → Continue gates on both grants. Refuse to proceed past wizard until both granted; return to wizard on revoke.
  Acceptance: PRD epic A all acceptance criteria met. Schema-valid bundles round-trip cleanly. Schema-invalid bundles fail with actionable errors.
  Verify: Reset macRo permissions in System Settings. Run app fresh. Walk through wizard. Grant both. Verify Library view appears (empty for now). Quit. Revoke Accessibility. Re-launch. Confirm wizard re-appears, deep-link button opens correct settings pane.

- [x] **4. Native services wrappers — SCK + CGEventTap + AVFoundation + Window detection** *(complete. `App/macRo/Native/SCKCapture.swift` (SCStream + SCContentFilter, BGRA frames on dedicated SCK queue, findWindow bridges WindowDetector.Match → SCWindow), `App/macRo/Native/EventTap.swift` (annotated-session listenOnly tap on dedicated thread, RawInputEvent + SynthInputEvent enums local to wrapper, full allocate→enable→disable→invalidate lifecycle via Unmanaged-boxed callback context, CGEvent.post via .cghidEventTap), `App/macRo/Native/Encoder.swift` (AVAssetWriter wrapping H.264/mov, deferred startSession until first sample PTS, async finish), `App/macRo/Native/WindowDetector.swift` (NSWorkspace + AXUIElement traversal, returns Cocoa-coord NSRect, throws only on regex-compile fail — per-app AX denials silently skipped). 4 happy-path tests in `App/macRoTests/NativeServicesTests.swift` (+4 supporting tests = 8 native total). 13 tests pass; 3 skipped cleanly (Screen Recording + Accessibility cannot be granted from test runner). BUILD SUCCEEDED + TEST SUCCEEDED. Codegen template fix piggy-backed: `Swift.Encoder` / `Swift.Decoder` qualified in `MacroFormat.swift` so domain `Encoder` (and any future shadowed name) doesn't break Codable conformance. Manual verification owed-to-Estevan: open Roblox, programmatically trigger SCKCapture against the Roblox window for 5s, confirm output mov plays in QuickTime — wire as debug-only Settings → Test Capture later, or invoke from Xcode REPL.)*
  Spec ref: `spec.md > Native services layer`
  What to build: `App/macRo/Native/SCKCapture.swift` — minimal SCK session capturing the Roblox window content rect at observed FPS, delegate-based frame callbacks. `App/macRo/Native/EventTap.swift` — record path (CGEventTap with annotated session, dedicated thread) + synthesize path (`CGEvent.post` with `.cghidEventTap`); strict no-leak lifecycle (allocate, enable, disable, invalidate). `App/macRo/Native/Encoder.swift` — `AVAssetWriter` consuming SCK frames into `mov`. `App/macRo/Native/WindowDetector.swift` — `NSWorkspace` + `AXUIElementCreateApplication` traversal to match `target.windowClass[]` + `target.windowTitleMatch`; returns `NSRect` content rect of matched window. Unit tests for happy path of each.
  Acceptance: SCK captures a Roblox window for 5s. EventTap records 5s of mixed input then can replay it cleanly. Encoder writes a valid 5s `mov`. WindowDetector returns nil when Roblox closed, valid rect when open.
  Verify: Open Roblox. Run `swift test --filter NativeServicesTests` — all pass. Manual: trigger 5s capture, confirm output `mov` plays in QuickTime.

- [x] **5. Playback engine — split into 5a / 5b / 5c on 2026-05-04 per /build "When Something Breaks" protocol**

  *Original scope (Engine.swift + RunHUD + BindingMismatchPrompt + App.swift wiring + tests + commit) was a single dispatch but timed out at 64-min wall-clock with zero on-disk output. Splitting honors the `sequence_revised: medium` friction trigger from the SKILL — finer dispatch granularity, same architectural plan. Three pre-existing untracked support files (`AppShortcutMonitor.swift`, `EngineErrors.swift`, `EngineLogger.swift`) committed as part of 5a.*

  Spec ref: `spec.md > Engine` + `prd.md > Epic D` (whole epic spans 5a/5b/5c).
  Acceptance (whole item 5): PRD epic D all acceptance criteria met. Hand-authored bundle from item 6 plays. Abort hotkey works regardless of frontmost app. Safety rules enforced (verifiable via log inspection). Resolution scaling clamps to rect.
  Verify (whole item 5): Hand-author a tiny `.macro` (a few clicks + an image gate). Open Roblox to known UI state. Run engine. Confirm clicks land in Roblox, not on macRo. Mid-run, hit `⌃⌥⌘.` — engine aborts within 200ms. Inspect log file — every synth event recorded. (Owed at item 6's polished fixtures.)
  **Status:** complete @ `b8d51ad` — 5a + 5b + 5c shipped in a single commit per the split's commit-once contract. Engine.swift (1,494 LoC) + RunHUD.swift (533 LoC) + BindingMismatchPrompt.swift (334 LoC) + AppShortcutMonitor + EngineErrors + EngineLogger + MacRoTheme deltas + App.swift wiring + 3 in-process EngineTests. xcodebuild BUILD SUCCEEDED + TEST SUCCEEDED on x86_64 host (Universal builds for Release). Visual cmd+R run-against-Roblox verification owed-to-Estevan at item 6's polished fixtures.

- [x] **5a. `Engine.swift` core — run loop + state machine + chokepoint + gates + schedule + stopOn + sub-stack + scaling**
  Spec ref: `spec.md > Engine` + `prd.md > Epic D` (run-loop / safety / pre-flight stories)
  What to build: `App/macRo/Domain/Engine.swift` — `Engine.shared` singleton-style class. State machine (`idle → preflight → (paused) → running ↔ gating → finished | aborted | failed`). Single dedicated serial queue. Pre-flight: schema-version check, window match (10s wait), required-bindings (will surface `BindingMismatchPrompt` modal in 5b — for now, expose `pendingBindingsCheck` callback hook), schedule check, FPS sample (~1s via SCK), resolution-scale calc. Run loop walks timeline events: input events → translate window-relative coords → synth via single chokepoint method (`synthesize(_ event: CGEvent)` asserts `NSWorkspace.frontmost != macRo`, clamps to window content rect post-scale, logs every event via `EngineLogger`); gate events → SCK frame capture → Vision template match (~0.95 IMG / ~0.85 POS) → retry/backoff → onFail action; loop events → visit-count tracking, runaway threshold 100,000; invokeSub events → sub-stack push/pop. Stop conditions polled every 500ms in parallel. Frame-rate scaling: held-key durations × `recordedFrameRate / runtimeFrameRate`, capped 0.5x–2x. `abort(reason:)` callable from any thread (hops to engine queue). Use existing `EngineErrors.swift` + `EngineLogger.swift` as-is (already on disk uncommitted; commit them with this item).
  Acceptance: `Engine.shared` exists; pre-flight refuses unknown `schemaVersion` with `EngineError.unsupportedSchema`; chokepoint refuses to synth when macRo frontmost; abort cleanly transitions to `aborted` state; all timeline event kinds dispatched correctly. Compiles in isolation.
  Verify: `swift build` (or xcodebuild) succeeds. No UI yet — verification deferred to 5c.
  **Status:** complete — Engine.swift core landed as part of the parent item-5 commit `b8d51ad`. AppShortcutMonitor + EngineErrors + EngineLogger committed alongside.

- [x] **5b. UI — `RunHUD.swift` + `BindingMismatchPrompt.swift` + `App.swift` hotkey wiring + XcodeGen regen**
  Spec ref: `spec.md > RunHUD` + `prd.md > Epic D` (abort + binding-mismatch stories)
  What to build: `App/macRo/UI/RunHUD.swift` — floating SwiftUI overlay window. 3-2-1 countdown to start, current state indicator, stop button (calls `Engine.shared.abort(reason: .userStop)`), "abort: ⌃⌥⌘." hint text. All visuals reach through `MacRoTheme` — no hardcoded colors/fonts/spacing. Position remembered via `UserDefaults` keyed `macRo.RunHUD.position`. `App/macRo/UI/BindingMismatchPrompt.swift` — modal triggered by Engine pre-flight's binding-check callback. Lists expected `requires.bindings` from manifest with current-state status, "remember for this macro" checkbox (persisted in `UserDefaults` keyed on `macRo.bindings.<macro-id>`), "Continue anyway" + "Cancel" buttons. `App/macRo/App.swift` — instantiate `AppShortcutMonitor` once at `@main` init; hold reference for app lifetime. Update `App/project.yml` with the new files; run `cd App && xcodegen generate`. Invoke the `626labs-design` skill if any new tokens are needed in `MacRoTheme`.
  Acceptance: RunHUD renders without hardcoded values (all via `MacRoTheme`). BindingMismatchPrompt persists "remember" choice across app launches. Hotkey monitor is registered before Engine.shared is first accessed.
  Verify: `xcodebuild -project App/macRo.xcodeproj -scheme macRo build` SUCCEEDED.
  **Status:** complete — RunHUD + BindingMismatchPrompt + App.swift hotkey wiring + MacRoTheme additions landed as part of the parent item-5 commit `b8d51ad`. XcodeGen regen clean.

- [x] **5c. Smoke tests + xcodebuild verify + commit + checklist mark**
  Spec ref: `spec.md > Engine` (acceptance) + `prd.md > Epic D` (verify column)
  What to build: At least one in-process unit test in `App/macRoTests/EngineTests.swift` covering: (a) pre-flight refuses unknown `schemaVersion` with `EngineError.unsupportedSchema`; (b) chokepoint refuses to synth when frontmost-app mock returns macRo's bundle id; (c) `abort(reason: .userHotkey)` from any thread cleanly transitions state to `aborted` within 200ms. `xcodebuild test` should run them clean. Commit all of 5a + 5b + 5c + the three pre-existing untracked support files in a single commit with the `feat(engine): item 5 — ...` message format from the original prompt. Mark item 5 as `[x]` in `docs/checklist.md` with a status line referencing the commit SHA.
  Acceptance: `xcodebuild build` + `xcodebuild test` both succeed clean. Commit lands. Checklist `[x]`'d.
  Verify: `git log --oneline -3` shows the engine commit. `xcodebuild -scheme macRo test` exits 0. Visual cmd+R verification (run a hand-authored bundle, hit `⌃⌥⌘.`) is owed-to-Estevan after item 6's polished fixtures land.
  **Status:** complete — `App/macRoTests/EngineTests.swift` (3 tests) landed in parent item-5 commit `b8d51ad`. Tests (a) unsupportedSchemaVersion-refuses + (b) abort-from-background-thread-is-safe pass; test (c) frontmost-app safety rule xctSkips with TODO referencing the deferred `frontmostBundleProvider` seam (per 5c's "do not modify Engine.swift to add a seam" rule — flag for /iterate). 16 tests pass, 4 skipped (3 native-services permission gates + the deferred frontmost test). xcodebuild BUILD SUCCEEDED + TEST SUCCEEDED on x86_64 host.

- [x] **6. Hand-authored test bundles + Engine fixture suite — CHECKPOINT #1**
  Spec ref: `spec.md > .macro bundle format` + `prd.md > Epic D`
  What to build: Author 3 hand-written `.macro` bundles by hand (raw YAML + cropped PNG screenshots from PS99 to use as gate refs). Coverage: (a) simple click-loop with no gates, (b) click-with-image-gate that waits for a UI element to appear, (c) schedule + stopOn combo (e.g., "run 10pm-6am, stop on Mythic"). Place at `App/macRoTests/Fixtures/`. Add a smoke-test target that loads each, validates, runs each for ≥30s against a manually-positioned PS99 window or aborts cleanly with clear error if Roblox isn't running. Run all 3 against the `macro-bundle-validator` agent.
  Acceptance: Each fixture bundle parses cleanly via `MacroBundle.load()`, pre-flights cleanly, runs ≥30s against PS99. `macro-bundle-validator` reports no critical or substantive findings on any fixture.
  Verify: Open PS99 to a known state. Run each fixture from the smoke-test target. Observe expected behavior in PS99. Hit abort during long runs. **CHECKPOINT #1 — pause, summarize what items 1–6 produced, wait for builder green-light before proceeding to item 7.**
  **Status:** structural pass complete @ `aa92f8a` — empirical PS99 verification owed at CHECKPOINT #1. Three hand-authored fixtures landed at `App/macRoTests/Fixtures/` (`tiny-click-loop.macro`, `click-with-image-gate.macro`, `schedule-and-stopon.macro`). Stub PNGs (1x1 RGBA, 67 bytes) hold the gate slots until real PS99 captures replace them at CHECKPOINT #1. Validator checklist applied inline (no Agent/Task tool available in this dispatch surface) — all 3 pass clean (no critical or substantive findings). 3 new EngineTests added; xcodebuild test SUCCEEDED with 19 tests / 4 skipped / 0 failures on x86_64. The fixture-smoke tests assert (a) `MacroBundle.load(at:)` parses each bundle cleanly + (b) the engine's first-entry path transitions to `.preflight` then `.failed(.windowNotFound)` cleanly when Roblox is absent / sticky-terminal entry path returns `.captureFailed` single-run-contract rejection. `testUnsupportedSchemaVersionRefuses` widened to accept either path for the same reason. Project.yml gained a `Fixtures` exclude on `macRoTests` sources so the multi-fixture `.yaml` files don't collide in the test bundle's flattened resources directory; fixtures are read via `#filePath` at runtime.

- [x] **7. Recorder — capture flow + `RecorderHUD` + parallel streams + draft bundle handoff**
  Spec ref: `spec.md > Recorder` + `prd.md > Epic B`
  What to build: `App/macRo/Domain/Recorder.swift` — drives SCK + EventTap + Encoder in parallel, writes raw artifacts to a working dir (`raw-video.mov`, `raw-input.jsonl`, `snapshots/<timestamp>.png` at every input event AND every 1 second). `App/macRo/UI/RecorderHUD.swift` — draggable overlay (timer MM:SS, abort button, hotkey hint). Position remembered across sessions via `UserDefaults`. 3-2-1 countdown UI, escape-dismissable. Game-pick sheet (PS99 default + Untagged option, populated from PluginLoader once item 10 lands; for now hardcode PS99 + Untagged). On stop, finalize working dir → draft bundle, open EditorView. Hard rule: HUD's own clicks NEVER reach the input log (use `NSWindow.level = .floating` + level-filter in EventTap callback).
  Acceptance: PRD epic B all acceptance criteria met. Three streams in sync (timestamps align). HUD-clicks excluded from input log (verifiable by inspecting `raw-input.jsonl`).
  Verify: Start a new recording, do 30s of mixed input in Roblox. Stop. Inspect working dir — confirm video.mov + input.jsonl + snapshots/. EditorView opens (initial empty editor; full editor in next item). Inspect `input.jsonl` — confirm no HUD-coordinate events.
  **Status:** complete @ `e9c4950` — Recorder.swift (1,250 LoC), RecorderHUD.swift (572 LoC), GamePickSheet.swift (249 LoC), CountdownOverlay.swift (249 LoC), ContentView.swift (+151), MacRoTheme.swift (+20), App.swift abort-hotkey wiring extended to abort Recorder.shared in parallel with Engine.shared. RecorderTests.swift (167 LoC) — 3 smoke tests: testAbortFromBackgroundThreadIsSafe (pass), testStopRecordingWithoutStartReturnsError (pass), testStartRecordingRequiresAccessibilityPermission (xctSkip — AX granted on test host, needs permission-provider seam, deferred to /iterate per the same pattern as item 5c's frontmost-app fallback). Three pre-existing `try? encoder.finish()` calls in Recorder.swift (items 7a/7b uncommitted) needed `await` to compile — fixed in-place as a missing-keyword-class fix (no logic change). xcodebuild BUILD SUCCEEDED + TEST SUCCEEDED on x86_64 host. 22 tests / 2 skipped / 0 failures (was 19 / 4 skipped after item 6 — net +3 tests, -2 skips). Visual cmd+R verification of Start Recording → game pick → countdown → HUD → stop → "Reveal in Finder" alert flow is owed-to-Estevan as part of CHECKPOINT #1 + 7 empirical pass against PS99.

- [x] **7.5. Quick-loop save flow — zero-edit "record + loop" path for casual users**
  Spec ref: `prd.md > Epic B` + new UX directive 2026-05-05 (process-notes.md > /build > UX additions)
  What to build: After `Recorder.stopRecording()` finalizes a draft bundle, replace the current "Recording saved" alert with a **post-record sheet** (or follow-up modal) offering THREE explicit paths so the editor isn't required for trivial macros:
    (a) **Save** — saves the bundle as-is (single play, raw timeline). Current behavior.
    (b) **Save as loop** — wraps the timeline so it repeats from start to finish forever, with a user-set delay between iterations. Requires:
         * A small numeric input "Wait [N] seconds between iterations" (default 1.0, accepts decimals).
         * Behind the scenes: append a final `loop` event to `timeline.yaml` with `target: 0.0` (jump to start) and a new `delayMs` field (= input * 1000). This is a v1 schema addition: add optional `delayMs: Int?` to the `loop` event in `schema/macro.schema.yaml`, regen `MacroFormat.swift` via `bun run codegen`, update `Engine.swift`'s loop-event dispatch to honor `delayMs` (sleep on the engine queue between jump-to-target and resuming the loop).
         * Save the modified bundle. User-flow ends — they can go to Library and run.
    (c) **Open in Editor** — placeholder until item 8 lands. For now, opens Finder to reveal the bundle (current behavior). Item 8's editor takes over the action.
  The wedge: Estevan's product directive — *"if a user just recorded a simple throwaway macro, they go in themselves, get themselves to where they wanna be, start the macro recorder, hit a couple of keys that are linked to hotkeys in the game, and they just want that to run on a loop. It should be that easy. They should be able to put a timer between when it repeats the same actions without having to edit the whole thing."* Editor is for refinement; Save-as-loop is the casual happy path.
  Acceptance: After a recording, user sees a 3-option sheet (not just a single OK alert). "Save as loop" with N=2.0 produces a bundle whose timeline ends with a `loop {target: 0.0, delayMs: 2000}` event. Engine plays the bundle correctly: full playback → 2s wait → playback again, ad infinitum, abortable via `⌃⌥⌘.`. "Save" path matches today's behavior. "Open in Editor" shows a placeholder explaining item 8 lands the full editor.
  Verify: Record a 5s sequence (e.g., press `1`, wait 1s, press `2`). Choose "Save as loop" with delay 1.5s. Run it. Observe `1` press → `2` press → 1.5s wait → `1` press → `2` press → 1.5s wait — repeating. Hit `⌃⌥⌘.`. Engine aborts cleanly.
  **Status:** complete @ `4ec2af3` — schema gained optional `delayMs: Int?` on the `loop` event (backward-compatible; absent or zero matches v1 pre-7.5 immediate-jump behavior). Codegen regenerated `MacroFormat.swift`. `Engine.dispatchLoop` now `cancelAwareSleep`s before the jump when `delayMs > 0`; abort during the wait returns without moving the cursor and the run loop exits cleanly. `ContentView.swift` (+335 LoC) replaces the single "Recording saved" alert with `PostRecordSheet` — three explicit paths (Save / Save as loop / Open in Editor); Save-as-loop reveals an inline numeric input (default 1.0s, decimals OK) and modifies the saved bundle in place via `MacroBundle.load → append loop event → MacroBundle.save`. Open-in-Editor is a placeholder explaining item 8 lands the full editor. All visuals through `MacRoTheme`. New `testLoopEventDelayMsRoundTripsAndDispatches` asserts payload-slot existence, YAML round-trip, and engine pre-flight acceptance. xcodebuild BUILD SUCCEEDED + TEST executed 23 / passed 20 / skipped 2 (pre-existing) / failed 1 (`testClickWithImageGateFixtureLoadsAndPreflights`, verified pre-existing against pristine main `3dfafdc`). Empirical "delay-honored mid-run" verification against PS99 still owed at CHECKPOINT #1 alongside item 7's empirical pass.

- [ ] **8. Editor — VIDEO/MOVE/ACTIONS/GATES lanes + scrub/cut/refine + gate authoring + script view + save — CHECKPOINT #2**
  Spec ref: `spec.md > EditorView` + `prd.md > Epic C`
  What to build: `App/macRo/UI/EditorView.swift` — the iMovie-flavored timeline editor matching the locked v2 mockup at `.superpowers/brainstorm/13966-1777822898/content/editor-shape-v2.html`. Four lanes per spec § 5: VIDEO (kept blue / cut gray, drag-to-trim handles, click-to-cut), MOVE (held-key duration bars: green WASD, blue camera deltas), ACTIONS (click dots, boxed-glyph hotkeys), GATES (◇ POS magenta / ◆ IMG cyan markers). Inspector panel on right: edit timing (±100ms), swap "click x,y" for image-anchored, add jitter, delete (with paired-event handling). Image-trigger cropper (`+ Image trigger` toolbar button) using snapshot at current timestamp; saves to `gates/img-<id>.png`, inserts gate event. Position-trigger via same flow, `gateKind: pos`, `onFail: abort` default. Toolbar panels: `subs:` form, `stopOn:` form, `schedule:` form (small SwiftUI forms writing to YAML model). Script view: native `NSTextView` with YAML highlighting, schema-validation-as-you-type, lossless round-trip with timeline. Save flow: package working dir into final `.macro` bundle, drop unreferenced snapshots, write to `~/Library/Application Support/macRo/Library/<game-slug>/<macro-id>.macro`. Invoke `626labs-design` skill at first SwiftUI authoring beat for token-precision.
  Acceptance: PRD epic C all acceptance criteria met. All four lanes populate from a recording. Cuts compress playback time. Inspector edits round-trip. Image-trigger cropper produces usable gate refs. Script view round-trips losslessly.
  Verify: Load a recorded session from item 7. Trim front, drop image-trigger gate, refine an event in inspector, toggle to script view + back, save. Re-load saved bundle, confirm changes persisted. **CHECKPOINT #2 — pause, summarize editor + recorder integration, wait for builder green-light before item 9.**

- [ ] **9. Library — `LibraryView` + `LibraryStore` + remote feed + install/auto-update/rollback/delete — CHECKPOINT #3**
  Spec ref: `spec.md > LibraryStore` + `spec.md > LibraryView` + `prd.md > Epic E`
  What to build: `App/macRo/Domain/LibraryStore.swift` — local inventory reader (`~/Library/Application Support/macRo/Library/<game>/*.macro`) + remote feed reader (configurable URL, default `https://macros.626labs.com/feed.json`). Install flow: download bundle ZIP, verify SHA-256, unzip into local. Auto-update: poll feed at launch, prompt when newer `factoryPatchable: true` macro found. Per spec Open Issue #3: store SHA-256 of original bundle at install time in a sidecar file at `<bundle>.installhash`; before auto-update, compare against current bundle hash — if drifted, show "your local edits will be overwritten" warn-then-decide modal. Rollback: keep last 3 versions in `~/Library/Application Support/macRo/Library/.versions/<game>/<macro-id>/v<n>/`; rollback swaps active version pointer. Delete: bundle + all versions + sidecars. `App/macRo/UI/LibraryView.swift` — browse local + remote in a unified card grid with source badges, version, last-updated, factoryPatchable status. Settings pane: feed URL override, per-macro auto-update toggle. Friendly empty/no-network states.
  Acceptance: PRD epic E all acceptance criteria met. SHA-256 verification rejects tampered downloads. Remote feed unreachable shows local-only with note (no error modal). Local-edit detection produces warn-then-decide modal.
  Verify: Set feed URL to local test JSON file with one macro entry. Install — observe macro in Library. Modify locally (edit `manifest.yaml`), bump feed version, refresh — confirm warn-then-decide modal. Choose each path. Rollback. Delete. **CHECKPOINT #3 — pause, summarize library/install/update flow, wait for builder green-light before item 10.**

- [ ] **10. PluginLoader + PS99 plugin (plugin.yaml + 5 seed macros) + first-launch seed install**
  Spec ref: `spec.md > PluginLoader` + `spec.md > PS99 plugin` + design spec § 10 + `prd.md > Epic A first-launch`
  What to build: `App/macRo/Domain/PluginLoader.swift` — index plugins from three locations (app-bundled `games/`, user-installed `~/Library/Application Support/macRo/Plugins/`, URL-installable via CLI subcommand `macRo install plugin <url-or-git>`). Each plugin indexed by `placeId`, surfaced in the Recorder's "What game?" sheet. Resolve conflicts deterministically (app-bundled wins on tie). Trust model: unsigned community plugins show Gatekeeper-style first-launch warning. `games/pet-sim-99/plugin.yaml` with `placeId: 8737899170`, displayName, window matchers (Roblox class array + title regex), `defaultBindings` (Estevan briefs canonical PS99 keybindings here — flag in process-notes if not yet captured). Author 5 PS99 seed macros: `auto-hatch.macro` (long-running, inventory-sensitive, Mythic-interrupt demo), `auto-grind-biome-1.macro` (position-critical, MOVE-lane demo), `auto-rebirth.macro` (UI-cue-heavy, GATES demo), `auto-fuse-pets.macro` (`subs:` + `stopOn:` combo demo), `clan-battle-helper.macro` (Estevan briefs PS99 clan-battle mechanic). All 5 must pass `macro-bundle-validator` clean. First-launch behavior: copy seed macros into user library after wizard completes.
  Acceptance: PRD epic A first-launch behavior met. PluginLoader finds all 3 locations. All 5 seed macros validate clean. `auto-grind-biome-1.macro` runs in 60s against a freshly-loaded PS99 (the wow moment).
  Verify: Reset library + permissions. Fresh launch. Walk wizard. Confirm 5 seed macros appear tagged "Pet Simulator 99". Run `auto-grind-biome-1.macro` against PS99 — confirm visible play within 60s.

- [ ] **11. Distribution + release pipeline — Sparkle + GitHub Actions notarize + appcast + rollback**
  Spec ref: `spec.md > Distribution & release` + `spec.md > Deployment — Identity & Signing` + `prd.md > Epic F`
  What to build: Integrate Sparkle 2.x via SPM. Configure `Info.plist` with `SUFeedURL` → `https://626labs.github.io/macRo/appcast.xml`. Generate Sparkle EdDSA keypair via `generate_keys` Sparkle utility; store private key in 1Password under `macRo / Sparkle Private Key`; embed public key in `Info.plist`. `tools/release/notarize.sh` — local wrapper: `xcodebuild archive` → sign with Apple Developer ID → `xcrun notarytool submit --wait` → `xcrun stapler staple` → `hdiutil` to DMG. `.github/workflows/release.yml` — triggers on `push` of `v*` tag: imports cert from `MACOS_CERTIFICATE` secret, runs notarize.sh, uploads DMG as Release asset, runs `tools/release/generate-appcast.sh`, commits + pushes regenerated `appcast.xml` to `gh-pages` branch. `tools/release/generate-appcast.sh` — produces appcast XML from Git tags + Release asset metadata + EdDSA signatures. Pre-release flag respected by Sparkle. Rollback path: app keeps last 3 installed versions in `~/Library/Application Support/macRo/.versions/`; Sparkle Settings pane has a "downgrade" entry. CI fails loudly on any step break.
  Acceptance: PRD epic F all acceptance criteria met. Tag push produces notarized DMG attached to Release. Sparkle prompt fires on next-launch when newer version available. Pre-release flag skips for stable users. Released DMGs never deleted.
  Verify: Tag `v0.0.1`, push. Watch GitHub Actions workflow. Download DMG, install — opens without Gatekeeper warning. Tag `v0.0.2` after a small change, push. Open the v0.0.1 install — Sparkle prompts to update. Install update.

- [ ] **12. Documentation & security verification — FINAL**
  Spec ref: `prd.md > What We're Building` + `spec.md > all sections`
  What to build: Update `README.md` with v1 install instructions (download DMG from latest Release, drag to Applications, launch, walk wizard), screenshots of OnboardingView + EditorView + LibraryView, tech stack list, link to design spec, link to 626Labs design system. Update `CLAUDE.md` if any conventions changed during the build. Create `.env.example` listing GitHub Actions secrets needed for the release workflow with placeholder values + comments (real values stay in GitHub-hosted secrets / 1Password). **Secrets scan:** `git log --all -p | grep -iE "password|secret|api_key|sparkle.*private|developer_id|MACOS_CERTIFICATE"` — confirm no real secrets in history (false positives in README/docs are fine; flag any actual key material). **Dependency audit:** `swift package show-dependencies` for SwiftPM tree (Sparkle is the only third-party); verify no known CVEs against Sparkle's pinned version. **Input validation spot-check:** review file path handling for `.macro` install paths (no traversal — clamp to `~/Library/Application Support/macRo/Library/`), URL parsing for remote feed (HTTPS-only enforcement, no `file://` schemes accepted), YAML parsing (using a strict YAML loader, not JSON-eval-style). **Auth & access control:** N/A (app has no auth surface — local-first by design). **Deployment security:** confirm GitHub Actions secrets are GitHub-hosted (`secrets.MACOS_CERTIFICATE`, etc.) not in repo; HTTPS enforced for feed + appcast; debug logging off in release builds; Sparkle EdDSA signature verification enforced (refuses unsigned updates). Push final commit to `main`. Tag `v0.1.0` for first public release.
  Acceptance: README is complete, accurate, and lets a fresh-clone user install + run macRo. `.env.example` exists. Secrets scan clean (or false positives documented). Dependency audit shows no unaddressed critical findings. Security spot-check documented in `process-notes.md`. Release tag `v0.1.0` exists and triggered the release workflow successfully.
  Verify: From a clean checkout, follow README install steps — confirm app launches and onboarding works. Run `git log --all -p | grep -iE "password|secret|api_key|sparkle.*private"` — confirm no real secrets, only documentation false positives. Confirm `v0.1.0` Release page on GitHub shows notarized DMG attachment.

## Gut-check

- **12 items total** — meets the SKILL's 8–12 target.
- **Three checkpoints** at items 6, 8, 9 — covers the riskiest transitions (engine-stable, editor-integrates-recorder, library-flow-end-to-end).
- **Final item is documentation + security per the contract.**
- **Estimated effort:** items 1-3 are setup-heavy but small (1 session each); items 4-5 are dense Swift / Apple-API work (longest items, may take 2 sessions each in iterative-prototype mode); items 6, 8, 9 include verification beats; item 11 is CI / release infra (well-trodden path); item 12 is docs polish.
- **Builder's `build_mode_preference: iterative-prototype`** harmonizes with autonomous mode here: the agent ships a working iteration of each item, the checkpoint pauses are the natural iteration boundaries.
